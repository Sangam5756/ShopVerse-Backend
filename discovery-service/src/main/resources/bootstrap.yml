spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        initial-interval: 2000
        max-interval: 5000
        max-attempts: 3
        multiplier: 1.5